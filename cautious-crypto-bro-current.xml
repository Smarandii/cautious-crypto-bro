This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
scripts/
  list_telegram_dialogs.py
src/
  cautious_crypto_bro/
    __init__.py
    approval_bot.py
    bybit.py
    config.py
    domain.py
    main.py
    openrouter.py
    service.py
    storage.py
    telegram_source.py
tests/
  test_approval_render.py
  test_domain.py
.dockerignore
.env.example
.gitignore
cautious-crypto-bro-cleanup.patch
docker-compose.yaml
Dockerfile
pyproject.toml
README.md
repomix-output.xml
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="scripts/list_telegram_dialogs.py">
import asyncio
import os

from telethon import TelegramClient


async def main() -> None:
    api_id = int(os.environ["TELEGRAM_API_ID"])
    api_hash = os.environ["TELEGRAM_API_HASH"]
    session_name = os.environ.get(
        "TELEGRAM_SESSION_NAME",
        "/state/cautious_crypto_bro",
    )

    client = TelegramClient(
        session_name,
        api_id,
        api_hash,
    )

    await client.start()

    print()
    print(
        f"{'ID':>18}  "
        f"{'TYPE':<16}  "
        f"{'USERNAME':<30}  "
        f"NAME"
    )
    print("-" * 100)

    async for dialog in client.iter_dialogs():
        entity = dialog.entity
        username = getattr(entity, "username", None)

        print(
            f"{dialog.id:>18}  "
            f"{type(entity).__name__:<16}  "
            f"{('@' + username) if username else '-':<30}  "
            f"{dialog.name}"
        )

    await client.disconnect()


if __name__ == "__main__":
    asyncio.run(main())
</file>

<file path="src/cautious_crypto_bro/__init__.py">
__all__ = []
</file>

<file path="src/cautious_crypto_bro/approval_bot.py">
from __future__ import annotations

import html
import logging
from datetime import datetime, timezone
from uuid import UUID

from aiogram import Bot, Dispatcher, F, Router
from aiogram.filters.callback_data import CallbackData
from aiogram.types import CallbackQuery, InlineKeyboardButton, InlineKeyboardMarkup

from .bybit import BybitDemoExecutor
from .domain import EntryType, IntentStatus, TradingIntent
from .storage import IntentStore

logger = logging.getLogger(__name__)


class IntentAction(CallbackData, prefix="intent"):
    action: str
    intent_id: str


class ApprovalBot:
    def __init__(self, *, token: str, approval_chat_id: int, approver_user_id: int,
                 max_age_seconds: int, store: IntentStore, executor: BybitDemoExecutor) -> None:
        self._bot = Bot(token=token)
        self._dispatcher = Dispatcher()
        self._router = Router()
        self._dispatcher.include_router(self._router)
        self._approval_chat_id = approval_chat_id
        self._approver_user_id = approver_user_id
        self._max_age_seconds = max_age_seconds
        self._store = store
        self._executor = executor

        self._router.callback_query(IntentAction.filter(F.action == "execute"))(self._execute)
        self._router.callback_query(IntentAction.filter(F.action == "skip"))(self._skip)

    async def start(self) -> None:
        await self._bot.delete_webhook(drop_pending_updates=False)

    async def run(self) -> None:
        await self._dispatcher.start_polling(self._bot)

    async def close(self) -> None:
        await self._bot.session.close()

    async def send_intent(self, intent: TradingIntent) -> None:
        keyboard = InlineKeyboardMarkup(inline_keyboard=[[
            InlineKeyboardButton(
                text="Execute",
                callback_data=IntentAction(action="execute", intent_id=str(intent.intent_id)).pack(),
                style="success",
            ),
            InlineKeyboardButton(
                text="Skip",
                callback_data=IntentAction(action="skip", intent_id=str(intent.intent_id)).pack(),
                style="danger",
            ),
        ]])
        await self._bot.send_message(
            chat_id=self._approval_chat_id,
            text=self._render(intent),
            parse_mode="HTML",
            disable_web_page_preview=True,
            reply_markup=keyboard,
        )

    async def _execute(self, callback: CallbackQuery, callback_data: IntentAction) -> None:
        if callback.from_user.id != self._approver_user_id:
            await callback.answer("Not authorized", show_alert=True)
            return
        await callback.answer()

        intent_id = UUID(callback_data.intent_id)
        intent = await self._store.get_intent(intent_id)
        if intent is None:
            await callback.answer("Intent no longer exists", show_alert=True)
            return
        if intent.status is not IntentStatus.PENDING:
            await callback.answer(f"Already {intent.status.value.lower()}", show_alert=True)
            return

        age = (datetime.now(timezone.utc) - intent.created_at).total_seconds()
        if age > self._max_age_seconds:
            await callback.answer(f"Intent is stale ({int(age)}s). Not executed.", show_alert=True)
            return

        if not await self._store.claim_for_execution(intent_id, callback.from_user.id):
            await callback.answer("Intent was already handled", show_alert=True)
            return

        try:
            order_id = await self._executor.execute(intent)
        except Exception as exc:
            logger.exception("Execution failed for %s", intent_id)
            await self._store.mark_failed(intent_id, str(exc))
            await self._edit_card(callback, intent, f"\n\n<b>FAILED</b>\n<code>{html.escape(str(exc))}</code>")
            return

        await self._store.mark_executed(intent_id, order_id)
        await self._edit_card(
            callback, intent,
            f"\n\n<b>EXECUTED ON BYBIT DEMO</b>\nOrder ID: <code>{html.escape(order_id)}</code>"
        )

    async def _skip(self, callback: CallbackQuery, callback_data: IntentAction) -> None:
        if callback.from_user.id != self._approver_user_id:
            await callback.answer("Not authorized", show_alert=True)
            return
        await callback.answer()

        intent_id = UUID(callback_data.intent_id)
        intent = await self._store.get_intent(intent_id)
        if intent is None:
            await callback.answer("Intent no longer exists", show_alert=True)
            return
        if not await self._store.mark_skipped(intent_id, callback.from_user.id):
            await callback.answer("Intent was already handled", show_alert=True)
            return
        await self._edit_card(callback, intent, "\n\n<b>SKIPPED</b>")

    async def _edit_card(self, callback: CallbackQuery, intent: TradingIntent, suffix: str) -> None:
        if callback.message is not None:
            await callback.message.edit_text(
                self._render(intent) + suffix,
                parse_mode="HTML",
                disable_web_page_preview=True,
                reply_markup=None,
            )

    @staticmethod
    def _render(intent: TradingIntent) -> str:
        source_url = intent.source.telegram_url
        source_line = (
            f'<a href="{html.escape(source_url)}">Open source message</a>'
            if source_url else "Source link unavailable"
        )
        entry = "Market" if intent.entry.type is EntryType.MARKET else f"{intent.entry.price:g}"

        risk_block = ""
        if intent.entry.price is not None:
            e = intent.entry.price
            if intent.side.value == "LONG":
                risk = (e - intent.stop_loss) / e * 100
                reward = (intent.take_profit - e) / e * 100
            else:
                risk = (intent.stop_loss - e) / e * 100
                reward = (e - intent.take_profit) / e * 100
            rr = reward / risk if risk > 0 else 0
            risk_block = (
                f"\nRisk to SL: <b>{risk:.2f}%</b>"
                f"\nReward to TP: <b>{reward:.2f}%</b>"
                f"\nR:R: <b>{rr:.2f}</b>"
            )

        return (
            f"<b>{html.escape(intent.side.value)} {html.escape(intent.symbol)}</b>\n\n"
            f"Entry: <b>{html.escape(entry)}</b>\n"
            f"Stop loss: <b>{intent.stop_loss:g}</b>\n"
            f"Take profit: <b>{intent.take_profit:g}</b>"
            f"{risk_block}\n\n"
            f"Confidence: <b>{intent.confidence:.0%}</b>\n"
            f"Thesis: {html.escape(intent.summary)}\n\n"
            f"Trader: {html.escape(intent.source.channel_title)}\n"
            f"Published: {html.escape(intent.source.published_at.isoformat())}\n"
            f"{source_line}"
        )
</file>

<file path="src/cautious_crypto_bro/bybit.py">
from __future__ import annotations

import asyncio
from decimal import Decimal, ROUND_DOWN

from pybit.unified_trading import HTTP

from .domain import EntryType, Side, TradingIntent


class TradeExecutionError(RuntimeError):
    pass


class BybitDemoExecutor:
    def __init__(self, *, api_key: str, api_secret: str, notional_usdt: float) -> None:
        self._session = HTTP(testnet=False, demo=True, api_key=api_key, api_secret=api_secret)
        self._notional_usdt = Decimal(str(notional_usdt))

    async def execute(self, intent: TradingIntent) -> str:
        return await asyncio.to_thread(self._execute_sync, intent)

    def _execute_sync(self, intent: TradingIntent) -> str:
        market_price = self._last_price(intent.symbol)
        sizing_price = Decimal(str(intent.entry.price)) if intent.entry.type is EntryType.LIMIT else market_price
        qty = self._quantity_for_notional(intent.symbol, sizing_price)

        params: dict[str, object] = {
            "category": "linear",
            "symbol": intent.symbol,
            "side": "Buy" if intent.side is Side.LONG else "Sell",
            "orderType": "Market" if intent.entry.type is EntryType.MARKET else "Limit",
            "qty": self._fmt(qty),
            "takeProfit": self._fmt(Decimal(str(intent.take_profit))),
            "stopLoss": self._fmt(Decimal(str(intent.stop_loss))),
            "tpslMode": "Full",
            "tpOrderType": "Market",
            "slOrderType": "Market",
            "timeInForce": "GTC",
        }
        if intent.entry.type is EntryType.LIMIT:
            params["price"] = self._fmt(Decimal(str(intent.entry.price)))

        response = self._session.place_order(**params)
        if response.get("retCode") != 0:
            raise TradeExecutionError(f"Bybit rejected order: {response.get('retCode')} {response.get('retMsg')}")
        order_id = response.get("result", {}).get("orderId")
        if not order_id:
            raise TradeExecutionError("Bybit returned success without orderId")
        return str(order_id)

    def _last_price(self, symbol: str) -> Decimal:
        response = self._session.get_tickers(category="linear", symbol=symbol)
        items = response.get("result", {}).get("list", [])
        if not items:
            raise TradeExecutionError(f"No Bybit ticker found for {symbol}")
        return Decimal(items[0]["lastPrice"])

    def _quantity_for_notional(self, symbol: str, price: Decimal) -> Decimal:
        response = self._session.get_instruments_info(category="linear", symbol=symbol)
        items = response.get("result", {}).get("list", [])
        if not items:
            raise TradeExecutionError(f"No Bybit instrument found for {symbol}")

        lot = items[0]["lotSizeFilter"]
        step = Decimal(lot["qtyStep"])
        min_qty = Decimal(lot["minOrderQty"])
        min_notional = Decimal(lot.get("minNotionalValue", "0"))

        raw = self._notional_usdt / price
        qty = (raw / step).to_integral_value(rounding=ROUND_DOWN) * step
        if qty < min_qty:
            raise TradeExecutionError(f"Configured notional too small: qty {qty} < minOrderQty {min_qty}")
        if min_notional and qty * price < min_notional:
            raise TradeExecutionError(f"Configured notional too small: {qty * price} < minNotionalValue {min_notional}")
        return qty

    @staticmethod
    def _fmt(value: Decimal) -> str:
        return format(value.normalize(), "f")
</file>

<file path="src/cautious_crypto_bro/config.py">
from __future__ import annotations

from functools import lru_cache
from pathlib import Path

from pydantic import field_validator
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", extra="ignore")

    telegram_api_id: int
    telegram_api_hash: str
    telegram_session_name: str = "cautious_crypto_bro"
    telegram_source_channels: list[str | int]

    telegram_bot_token: str
    telegram_approver_user_id: int
    telegram_approval_chat_id: int

    openrouter_api_key: str
    openrouter_model: str = "google/gemma-4-26b-a4b-it"
    openrouter_base_url: str = "https://openrouter.ai/api/v1"

    bybit_api_key: str
    bybit_api_secret: str
    bybit_default_notional_usdt: float = 25.0

    database_path: Path = Path("data/cautious_crypto_bro.sqlite3")
    intent_max_age_seconds: int = 900
    log_level: str = "INFO"

    @field_validator("telegram_source_channels", mode="before")
    @classmethod
    def parse_channels(cls, value: object) -> object:
        if isinstance(value, str):
            channels: list[str | int] = []

            for raw in value.split(","):
                item = raw.strip()
                if not item:
                    continue

                if item.lstrip("-").isdigit():
                    channels.append(int(item))
                else:
                    channels.append(item.lstrip("@"))

            return channels

        return value

    @field_validator("telegram_source_channels")
    @classmethod
    def require_channels(
        cls,
        value: list[str | int],
    ) -> list[str | int]:
        if not value:
            raise ValueError("TELEGRAM_SOURCE_CHANNELS must contain at least one channel")
        return value

    @field_validator("bybit_default_notional_usdt")
    @classmethod
    def positive_notional(cls, value: float) -> float:
        if value <= 0:
            raise ValueError("BYBIT_DEFAULT_NOTIONAL_USDT must be > 0")
        return value


@lru_cache
def get_settings() -> Settings:
    return Settings()
</file>

<file path="src/cautious_crypto_bro/domain.py">
from __future__ import annotations

from datetime import datetime, timezone
from enum import StrEnum
from uuid import UUID, uuid4

from pydantic import BaseModel, ConfigDict, Field, model_validator


class Side(StrEnum):
    LONG = "LONG"
    SHORT = "SHORT"


class EntryType(StrEnum):
    MARKET = "MARKET"
    LIMIT = "LIMIT"


class IntentStatus(StrEnum):
    PENDING = "PENDING"
    EXECUTING = "EXECUTING"
    EXECUTED = "EXECUTED"
    SKIPPED = "SKIPPED"
    FAILED = "FAILED"


class SourceMessage(BaseModel):
    model_config = ConfigDict(extra="forbid")
    channel_id: int
    channel_title: str
    channel_username: str | None = None
    message_id: int
    published_at: datetime
    received_at: datetime
    text: str

    @property
    def telegram_url(self) -> str | None:
        if self.channel_username:
            return f"https://t.me/{self.channel_username.lstrip('@')}/{self.message_id}"
        return None


class Entry(BaseModel):
    model_config = ConfigDict(extra="forbid")
    type: EntryType
    price: float | None = Field(default=None, gt=0)

    @model_validator(mode="after")
    def validate_price(self) -> "Entry":
        if self.type is EntryType.LIMIT and self.price is None:
            raise ValueError("LIMIT entry requires a price")
        if self.type is EntryType.MARKET and self.price is not None:
            raise ValueError("MARKET entry must not contain a price")
        return self


class ExtractedIntent(BaseModel):
    model_config = ConfigDict(extra="forbid")
    symbol: str
    side: Side
    entry: Entry
    stop_loss: float = Field(gt=0)
    take_profit: float = Field(gt=0)
    summary: str = Field(min_length=1, max_length=500)
    confidence: float = Field(ge=0, le=1)


class IntentExtraction(BaseModel):
    model_config = ConfigDict(extra="forbid")
    actionable: bool
    reason: str = Field(min_length=1, max_length=500)
    intent: ExtractedIntent | None = None

    @model_validator(mode="after")
    def consistent_actionability(self) -> "IntentExtraction":
        if self.actionable and self.intent is None:
            raise ValueError("actionable=true requires intent")
        if not self.actionable and self.intent is not None:
            raise ValueError("actionable=false requires intent=null")
        return self


class TradingIntent(BaseModel):
    model_config = ConfigDict(extra="forbid")

    intent_id: UUID = Field(default_factory=uuid4)
    source: SourceMessage
    symbol: str
    side: Side
    entry: Entry
    stop_loss: float = Field(gt=0)
    take_profit: float = Field(gt=0)
    summary: str = Field(min_length=1, max_length=500)
    confidence: float = Field(ge=0, le=1)
    created_at: datetime = Field(default_factory=lambda: datetime.now(timezone.utc))
    status: IntentStatus = IntentStatus.PENDING

    @model_validator(mode="after")
    def validate_trade_geometry(self) -> "TradingIntent":
        normalized = self.symbol.upper().replace("/", "").replace("-", "")
        object.__setattr__(self, "symbol", normalized)
        if not normalized.endswith("USDT"):
            raise ValueError("MVP supports only USDT linear symbols")

        reference = self.entry.price
        if reference is not None:
            if self.side is Side.LONG and not self.stop_loss < reference < self.take_profit:
                raise ValueError("LONG requires stop_loss < entry < take_profit")
            if self.side is Side.SHORT and not self.take_profit < reference < self.stop_loss:
                raise ValueError("SHORT requires take_profit < entry < stop_loss")
        else:
            if self.side is Side.LONG and not self.stop_loss < self.take_profit:
                raise ValueError("LONG requires stop_loss < take_profit")
            if self.side is Side.SHORT and not self.take_profit < self.stop_loss:
                raise ValueError("SHORT requires take_profit < stop_loss")
        return self
</file>

<file path="src/cautious_crypto_bro/main.py">
from __future__ import annotations

import asyncio
import logging

from .approval_bot import ApprovalBot
from .bybit import BybitDemoExecutor
from .config import get_settings
from .openrouter import OpenRouterIntentExtractor
from .service import SignalService
from .storage import IntentStore
from .telegram_source import TelegramSource


async def async_main() -> None:
    settings = get_settings()
    logging.basicConfig(
        level=getattr(logging, settings.log_level.upper(), logging.INFO),
        format="%(asctime)s %(levelname)s %(name)s: %(message)s",
    )

    store = IntentStore(settings.database_path)
    await store.initialize()

    extractor = OpenRouterIntentExtractor(
        api_key=settings.openrouter_api_key,
        model=settings.openrouter_model,
        base_url=settings.openrouter_base_url,
    )
    executor = BybitDemoExecutor(
        api_key=settings.bybit_api_key,
        api_secret=settings.bybit_api_secret,
        notional_usdt=settings.bybit_default_notional_usdt,
    )
    bot = ApprovalBot(
        token=settings.telegram_bot_token,
        approval_chat_id=settings.telegram_approval_chat_id,
        approver_user_id=settings.telegram_approver_user_id,
        max_age_seconds=settings.intent_max_age_seconds,
        store=store,
        executor=executor,
    )
    await bot.start()

    service = SignalService(store=store, extractor=extractor, approval_bot=bot)
    source = TelegramSource(
        api_id=settings.telegram_api_id,
        api_hash=settings.telegram_api_hash,
        session_name=settings.telegram_session_name,
        channels=settings.telegram_source_channels,
        on_message=service.on_message,
    )
    await source.start()

    try:
        async with asyncio.TaskGroup() as tg:
            tg.create_task(bot.run())
            tg.create_task(source.run_until_disconnected())
    finally:
        await source.disconnect()
        await bot.close()
        await extractor.close()


def main() -> None:
    asyncio.run(async_main())


if __name__ == "__main__":
    main()
</file>

<file path="src/cautious_crypto_bro/openrouter.py">
from __future__ import annotations

import logging
import httpx

from .domain import IntentExtraction, SourceMessage, TradingIntent

logger = logging.getLogger(__name__)

SYSTEM_PROMPT = """
You are a conservative crypto trading-signal parser.
Do not give trading advice and do not invent missing information.

Decide whether ONE Telegram post/caption contains one complete, immediately
actionable trade proposal representable by the supplied schema.

MVP rules:
- only USDT linear/perpetual-style symbols
- required: symbol, LONG/SHORT, entry semantics, stop loss, take profit
- MARKET only if the author clearly says enter now/at market or clearly states they entered now
- otherwise an explicit numeric entry price is required and entry.type must be LIMIT
- exactly one stop and one target
- never infer prices from vague phrases such as "below support", "previous low",
  "ATH", "the box", or information not present in the supplied text
- if the post is commentary, an update to an older idea, incomplete, ambiguous,
  or depends on unseen media, actionable=false
- confidence is extraction confidence, not probability of profit
- summary is one short sentence describing the trader's stated thesis
""".strip()


class OpenRouterIntentExtractor:
    def __init__(self, *, api_key: str, model: str, base_url: str) -> None:
        self._model = model
        self._client = httpx.AsyncClient(
            base_url=base_url.rstrip("/"),
            headers={"Authorization": f"Bearer {api_key}", "Content-Type": "application/json"},
            timeout=httpx.Timeout(45.0),
        )

    async def close(self) -> None:
        await self._client.aclose()

    async def extract(self, source: SourceMessage) -> TradingIntent | None:
        payload = {
            "model": self._model,
            "messages": [
                {"role": "system", "content": SYSTEM_PROMPT},
                {"role": "user", "content": (
                    f"Channel: {source.channel_title}\n"
                    f"Published: {source.published_at.isoformat()}\n\n"
                    f"Telegram post:\n{source.text}"
                )},
            ],
            "temperature": 0,
            "response_format": {
                "type": "json_schema",
                "json_schema": {
                    "name": "trading_intent_extraction",
                    "strict": True,
                    "schema": IntentExtraction.model_json_schema(),
                },
            },
        }
        response = await self._client.post("/chat/completions", json=payload)
        response.raise_for_status()
        content = response.json()["choices"][0]["message"]["content"]
        extraction = IntentExtraction.model_validate_json(content)

        if not extraction.actionable or extraction.intent is None:
            logger.info("No actionable intent for %s/%s: %s",
                        source.channel_id, source.message_id, extraction.reason)
            return None

        raw = extraction.intent
        return TradingIntent(
            source=source,
            symbol=raw.symbol,
            side=raw.side,
            entry=raw.entry,
            stop_loss=raw.stop_loss,
            take_profit=raw.take_profit,
            summary=raw.summary,
            confidence=raw.confidence,
        )
</file>

<file path="src/cautious_crypto_bro/service.py">
from __future__ import annotations

import logging

from .approval_bot import ApprovalBot
from .domain import SourceMessage
from .openrouter import OpenRouterIntentExtractor
from .storage import IntentStore

logger = logging.getLogger(__name__)


class SignalService:
    def __init__(self, *, store: IntentStore, extractor: OpenRouterIntentExtractor,
                 approval_bot: ApprovalBot) -> None:
        self._store = store
        self._extractor = extractor
        self._approval_bot = approval_bot

    async def on_message(self, source: SourceMessage) -> None:
        if not await self._store.save_source(source):
            logger.debug("Duplicate Telegram message %s/%s", source.channel_id, source.message_id)
            return
        try:
            intent = await self._extractor.extract(source)
        except Exception:
            logger.exception("Intent extraction failed for %s/%s", source.channel_id, source.message_id)
            return
        if intent is None:
            return
        await self._store.create_intent(intent)
        await self._approval_bot.send_intent(intent)
        logger.info("Created trading intent %s", intent.intent_id)
</file>

<file path="src/cautious_crypto_bro/storage.py">
from __future__ import annotations

from pathlib import Path
from uuid import UUID

import aiosqlite

from .domain import IntentStatus, SourceMessage, TradingIntent


class IntentStore:
    def __init__(self, database_path: Path) -> None:
        self._database_path = database_path

    async def initialize(self) -> None:
        self._database_path.parent.mkdir(parents=True, exist_ok=True)
        async with aiosqlite.connect(self._database_path) as db:
            await db.executescript(
                """
                PRAGMA journal_mode=WAL;
                CREATE TABLE IF NOT EXISTS source_messages (
                    channel_id INTEGER NOT NULL,
                    message_id INTEGER NOT NULL,
                    payload_json TEXT NOT NULL,
                    PRIMARY KEY (channel_id, message_id)
                );
                CREATE TABLE IF NOT EXISTS intents (
                    intent_id TEXT PRIMARY KEY,
                    channel_id INTEGER NOT NULL,
                    message_id INTEGER NOT NULL,
                    payload_json TEXT NOT NULL,
                    status TEXT NOT NULL,
                    decision_user_id INTEGER,
                    bybit_order_id TEXT,
                    error TEXT,
                    created_at TEXT NOT NULL,
                    updated_at TEXT NOT NULL
                );
                """
            )
            await db.commit()

    async def save_source(self, source: SourceMessage) -> bool:
        async with aiosqlite.connect(self._database_path) as db:
            cursor = await db.execute(
                """INSERT OR IGNORE INTO source_messages(channel_id, message_id, payload_json)
                   VALUES (?, ?, ?)""",
                (source.channel_id, source.message_id, source.model_dump_json()),
            )
            await db.commit()
            return cursor.rowcount == 1

    async def create_intent(self, intent: TradingIntent) -> None:
        now = intent.created_at.isoformat()
        async with aiosqlite.connect(self._database_path) as db:
            await db.execute(
                """INSERT INTO intents(
                       intent_id, channel_id, message_id, payload_json,
                       status, created_at, updated_at
                   ) VALUES (?, ?, ?, ?, ?, ?, ?)""",
                (
                    str(intent.intent_id),
                    intent.source.channel_id,
                    intent.source.message_id,
                    intent.model_dump_json(),
                    intent.status.value,
                    now,
                    now,
                ),
            )
            await db.commit()

    async def get_intent(self, intent_id: UUID) -> TradingIntent | None:
        async with aiosqlite.connect(self._database_path) as db:
            db.row_factory = aiosqlite.Row
            cursor = await db.execute(
                "SELECT payload_json, status FROM intents WHERE intent_id = ?",
                (str(intent_id),),
            )
            row = await cursor.fetchone()
        if row is None:
            return None
        intent = TradingIntent.model_validate_json(row["payload_json"])
        return intent.model_copy(update={"status": IntentStatus(row["status"])})

    async def claim_for_execution(self, intent_id: UUID, user_id: int) -> bool:
        async with aiosqlite.connect(self._database_path) as db:
            cursor = await db.execute(
                """UPDATE intents
                   SET status = ?, decision_user_id = ?, updated_at = CURRENT_TIMESTAMP
                   WHERE intent_id = ? AND status = ?""",
                (IntentStatus.EXECUTING.value, user_id, str(intent_id), IntentStatus.PENDING.value),
            )
            await db.commit()
            return cursor.rowcount == 1

    async def mark_skipped(self, intent_id: UUID, user_id: int) -> bool:
        async with aiosqlite.connect(self._database_path) as db:
            cursor = await db.execute(
                """UPDATE intents
                   SET status = ?, decision_user_id = ?, updated_at = CURRENT_TIMESTAMP
                   WHERE intent_id = ? AND status = ?""",
                (IntentStatus.SKIPPED.value, user_id, str(intent_id), IntentStatus.PENDING.value),
            )
            await db.commit()
            return cursor.rowcount == 1

    async def mark_executed(self, intent_id: UUID, order_id: str) -> None:
        await self._set_terminal(intent_id, IntentStatus.EXECUTED, bybit_order_id=order_id)

    async def mark_failed(self, intent_id: UUID, error: str) -> None:
        await self._set_terminal(intent_id, IntentStatus.FAILED, error=error[:2000])

    async def _set_terminal(
        self,
        intent_id: UUID,
        status: IntentStatus,
        *,
        bybit_order_id: str | None = None,
        error: str | None = None,
    ) -> None:
        async with aiosqlite.connect(self._database_path) as db:
            await db.execute(
                """UPDATE intents
                   SET status = ?, bybit_order_id = ?, error = ?, updated_at = CURRENT_TIMESTAMP
                   WHERE intent_id = ?""",
                (status.value, bybit_order_id, error, str(intent_id)),
            )
            await db.commit()
</file>

<file path="src/cautious_crypto_bro/telegram_source.py">
from __future__ import annotations

import logging
from collections.abc import Awaitable, Callable
from datetime import datetime, timezone

from telethon import TelegramClient, events
from telethon.tl.custom.message import Message

from .domain import SourceMessage

logger = logging.getLogger(__name__)
MessageHandler = Callable[[SourceMessage], Awaitable[None]]


class TelegramSource:
    def __init__(self, *, api_id: int, api_hash: str, session_name: str,
                 channels: list[str | int], on_message: MessageHandler) -> None:
        self._client = TelegramClient(session_name, api_id, api_hash)
        self._channels = channels
        self._on_message = on_message

    async def start(self) -> None:
        await self._client.start()
        entities = []
        for channel in self._channels:
            entity = await self._client.get_entity(channel)
            entities.append(entity)
            logger.info("Watching Telegram source: %s", channel)

        @self._client.on(events.NewMessage(chats=entities))
        async def handle(event: events.NewMessage.Event) -> None:
            source = self._convert(event.message)
            if source is not None:
                await self._on_message(source)

    async def run_until_disconnected(self) -> None:
        await self._client.run_until_disconnected()

    async def disconnect(self) -> None:
        await self._client.disconnect()

    @staticmethod
    def _convert(message: Message) -> SourceMessage | None:
        # First vertical slice supports text and media captions only.
        text = (message.message or "").strip()
        if not text:
            logger.info("Ignoring message %s: no text/caption in MVP", message.id)
            return None
        chat = message.chat
        if chat is None:
            return None

        published_at = message.date
        if published_at.tzinfo is None:
            published_at = published_at.replace(tzinfo=timezone.utc)

        return SourceMessage(
            channel_id=message.chat_id,
            channel_title=getattr(chat, "title", None) or str(message.chat_id),
            channel_username=getattr(chat, "username", None),
            message_id=message.id,
            published_at=published_at,
            received_at=datetime.now(timezone.utc),
            text=text,
        )
</file>

<file path="tests/test_approval_render.py">
from datetime import datetime, timezone

from cautious_crypto_bro.approval_bot import ApprovalBot
from cautious_crypto_bro.domain import Entry, EntryType, Side, SourceMessage, TradingIntent


def test_render_contains_decision_context() -> None:
    intent = TradingIntent(
        source=SourceMessage(
            channel_id=-100123,
            channel_title="Trader & Co",
            channel_username="trader",
            message_id=99,
            published_at=datetime(2026, 9, 12, 18, 0, tzinfo=timezone.utc),
            received_at=datetime(2026, 9, 12, 18, 0, 1, tzinfo=timezone.utc),
            text="signal",
        ),
        symbol="ETHUSDT", side=Side.LONG,
        entry=Entry(type=EntryType.LIMIT, price=4000),
        stop_loss=3900, take_profit=4300,
        summary="Bounce from support.", confidence=0.9,
    )
    rendered = ApprovalBot._render(intent)
    assert "LONG ETHUSDT" in rendered
    assert "R:R:" in rendered
    assert "Open source message" in rendered
    assert "Trader &amp; Co" in rendered
</file>

<file path="tests/test_domain.py">
from datetime import datetime, timezone
import pytest
from pydantic import ValidationError

from cautious_crypto_bro.domain import Entry, EntryType, Side, SourceMessage, TradingIntent


def source() -> SourceMessage:
    return SourceMessage(
        channel_id=-100123,
        channel_title="Trader",
        channel_username="trader",
        message_id=42,
        published_at=datetime.now(timezone.utc),
        received_at=datetime.now(timezone.utc),
        text="LONG BTC 100k SL 98k TP 105k",
    )


def test_valid_long_limit_intent() -> None:
    intent = TradingIntent(
        source=source(), symbol="BTC/USDT", side=Side.LONG,
        entry=Entry(type=EntryType.LIMIT, price=100_000),
        stop_loss=98_000, take_profit=105_000,
        summary="Long BTC from support.", confidence=0.95,
    )
    assert intent.symbol == "BTCUSDT"


def test_long_geometry_rejects_stop_above_entry() -> None:
    with pytest.raises(ValidationError):
        TradingIntent(
            source=source(), symbol="BTCUSDT", side=Side.LONG,
            entry=Entry(type=EntryType.LIMIT, price=100_000),
            stop_loss=101_000, take_profit=105_000,
            summary="Invalid", confidence=0.5,
        )


def test_limit_requires_price() -> None:
    with pytest.raises(ValidationError):
        Entry(type=EntryType.LIMIT)


def test_market_rejects_price() -> None:
    with pytest.raises(ValidationError):
        Entry(type=EntryType.MARKET, price=100)
</file>

<file path=".dockerignore">
.git
.gitignore
.env
.venv
__pycache__
*.py[cod]
.pytest_cache
*.session
*.session-journal
data
build
dist
*.egg-info
cautious-crypto-bro-architecture.svg
</file>

<file path=".env.example">
TELEGRAM_API_ID=
TELEGRAM_API_HASH=
TELEGRAM_SESSION_NAME=cautious_crypto_bro
TELEGRAM_SOURCE_CHANNELS=

TELEGRAM_BOT_TOKEN=
TELEGRAM_APPROVER_USER_ID=
TELEGRAM_APPROVAL_CHAT_ID=

OPENROUTER_API_KEY=
OPENROUTER_MODEL=google/gemma-4-26b-a4b-it
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

BYBIT_API_KEY=
BYBIT_API_SECRET=
BYBIT_DEFAULT_NOTIONAL_USDT=25

DATABASE_PATH=data/cautious_crypto_bro.sqlite3
INTENT_MAX_AGE_SECONDS=900
LOG_LEVEL=INFO
</file>

<file path=".gitignore">
.env
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
*.session
*.session-journal
data/
build/
dist/
*.egg-info/
</file>

<file path="cautious-crypto-bro-cleanup.patch">
diff --git a/.env.example b/.env.example
index 2212d85..42a60b3 100644
--- a/.env.example
+++ b/.env.example
@@ -1,7 +1,6 @@
 TELEGRAM_API_ID=
 TELEGRAM_API_HASH=
-TELEGRAM_SESSION_NAME=cautious_crypto_bro
-TELEGRAM_SOURCE_CHANNELS=
+TELEGRAM_SOURCE_CHANNELS=[-1001234567890]
 
 TELEGRAM_BOT_TOKEN=
 TELEGRAM_APPROVER_USER_ID=
@@ -9,12 +8,10 @@ TELEGRAM_APPROVAL_CHAT_ID=
 
 OPENROUTER_API_KEY=
 OPENROUTER_MODEL=google/gemma-4-26b-a4b-it
-OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
 
 BYBIT_API_KEY=
 BYBIT_API_SECRET=
 BYBIT_DEFAULT_NOTIONAL_USDT=25
 
-DATABASE_PATH=data/cautious_crypto_bro.sqlite3
 INTENT_MAX_AGE_SECONDS=900
-LOG_LEVEL=INFO
\ No newline at end of file
+LOG_LEVEL=INFO
diff --git a/README.md b/README.md
index 756ea97..072bf0c 100644
--- a/README.md
+++ b/README.md
@@ -1,137 +1,30 @@
 # Cautious Crypto Bro
 
-Weekend MVP: watch selected Telegram trader channels, turn live text/captions into structured trading intents, show those intents in an approval Telegram bot, and place a Bybit **Demo Trading** order only after explicit approval.
+Live Telegram trading ideas → structured `TradingIntent` → human approval → Bybit Demo order.
 
-## MVP scope
-
-Implemented:
-
-- live Telegram channel ingestion via a dedicated Telegram user session
-- text/caption signal interpretation through OpenRouter
-- strict Pydantic `TradingIntent` validation
-- Telegram approval cards with `Execute` / `Skip`
-- only the configured Telegram user can approve trades
-- Bybit Demo Trading execution
-- fixed configurable USDT notional sizing
-- quantity rounding to Bybit instrument rules
-- SL/TP passed with the order
-- SQLite persistence for source messages, intents, decisions, and execution result
-- stale-intent check before execution
-- idempotent state transition so the same intent cannot be executed twice
-
-Deliberately not implemented yet:
-
-- image / voice / video understanding
-- multi-message idea-thread reconstruction
-- edits to an existing trade
-- multiple take-profits / scaling
-- live position monitoring and PnL reporting
-- historical backtesting
-
-## Runtime model
-
-The application is Docker-first. You do not need a project-local Python installation or `.venv`.
-
-- `uv` resolves and installs Python dependencies inside Docker.
-- `uv.lock` is committed for reproducible builds.
-- Telegram session state and SQLite live in the Docker named volume `app_state`.
-- `.env` is the only local runtime configuration file and is gitignored.
-
-## Initial setup
-
-Create your local environment file:
+## Run
 
 ```bash
 cp .env.example .env
-```
+# Fill .env. TELEGRAM_SOURCE_CHANNELS is a JSON array, e.g. [-1002132062264]
 
-Fill in `.env` with Telegram, OpenRouter, and Bybit Demo credentials.
-
-Generate the lock file using the uv Docker image, so uv does not need to be installed on the host:
-
-```bash
-docker run --rm \
-  --user "$(id -u):$(id -g)" \
-  -v "$PWD:/workspace" \
-  -w /workspace \
-  ghcr.io/astral-sh/uv:0.10.0-python3.12-bookworm-slim \
-  uv lock
-```
-
-Build and test:
-
-```bash
 docker compose build
 docker compose --profile test run --rm test
+docker compose run --rm app   # first Telegram login / foreground run
 ```
 
-## First Telegram login
-
-Telethon needs one interactive login for the dedicated Telegram account. Run:
-
-```bash
-docker compose run --rm app
-```
-
-Enter the Telegram phone/code/password when prompted. The resulting session is saved in the Docker named volume, so subsequent containers reuse it.
-
-Stop the foreground process after authentication, then start normally:
+After login:
 
 ```bash
 docker compose up -d app
 docker compose logs -f app
 ```
 
-## Useful commands
-
-Run tests:
-
-```bash
-docker compose --profile test run --rm test
-```
-
-Rebuild after dependency or source changes:
-
-```bash
-docker compose build app
-```
-
-Run in foreground:
-
-```bash
-docker compose up app
-```
-
-Stop the application without deleting state:
-
-```bash
-docker compose down
-```
-
-Delete all local application state, including the Telegram session and SQLite database:
-
-```bash
-docker compose down -v
-```
-
-## Trading behavior
-
-The LLM is not allowed to place orders. It may only emit a `TradingIntent`.
-
-An intent must contain:
-
-- `symbol`
-- `side`
-- `entry`
-- `stop_loss`
-- `take_profit`
-- `summary`
-- confidence
-
-If the model decides the post is not an actionable trade, or cannot identify required trade parameters without inventing them, no approval card is created.
-
-`Execute` performs deterministic validation and then calls Bybit Demo Trading.
+Runtime state (Telethon session + SQLite) lives in the `app_state` Docker volume.
 
-## Important
+## MVP invariants
 
-This repository intentionally defaults to Bybit Demo Trading through `pybit(..., demo=True)`. Do not change this to live trading until the execution and validation layers have been deliberately redesigned and reviewed.
\ No newline at end of file
+- Demo trading only.
+- The LLM produces intents; only deterministic code can place orders.
+- Missing/ambiguous trade parameters are rejected rather than invented.
+- Required intent fields: symbol, side, entry, stop loss, take profit.
diff --git a/docker-compose.yaml b/docker-compose.yaml
index 2cd9918..984b261 100644
--- a/docker-compose.yaml
+++ b/docker-compose.yaml
@@ -3,16 +3,12 @@ services:
     build:
       context: .
       target: runtime
-    env_file:
-      - .env
+    env_file: .env
     environment:
-      # Keep mutable application state out of the source tree.
       TELEGRAM_SESSION_NAME: /state/cautious_crypto_bro
       DATABASE_PATH: /state/cautious_crypto_bro.sqlite3
     volumes:
       - app_state:/state
-    stdin_open: true
-    tty: true
     restart: unless-stopped
 
   test:
@@ -23,4 +19,4 @@ services:
     command: ["/app/.venv/bin/pytest", "-v"]
 
 volumes:
-  app_state:
\ No newline at end of file
+  app_state:
diff --git a/pyproject.toml b/pyproject.toml
index cf00cb9..ba22493 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -16,7 +16,6 @@ dependencies = [
 [dependency-groups]
 dev = [
   "pytest>=8.4,<9",
-  "pytest-asyncio>=1.1,<2",
 ]
 
 [project.scripts]
@@ -31,5 +30,4 @@ packages = ["src/cautious_crypto_bro"]
 
 [tool.pytest.ini_options]
 pythonpath = ["src"]
-asyncio_mode = "auto"
-testpaths = ["tests"]
\ No newline at end of file
+testpaths = ["tests"]
diff --git a/src/cautious_crypto_bro/approval_bot.py b/src/cautious_crypto_bro/approval_bot.py
index 06189ce..5a7d8d8 100644
--- a/src/cautious_crypto_bro/approval_bot.py
+++ b/src/cautious_crypto_bro/approval_bot.py
@@ -22,20 +22,33 @@ class IntentAction(CallbackData, prefix="intent"):
 
 
 class ApprovalBot:
-    def __init__(self, *, token: str, approval_chat_id: int, approver_user_id: int,
-                 max_age_seconds: int, store: IntentStore, executor: BybitDemoExecutor) -> None:
+    def __init__(
+        self,
+        *,
+        token: str,
+        approval_chat_id: int,
+        approver_user_id: int,
+        max_age_seconds: int,
+        store: IntentStore,
+        executor: BybitDemoExecutor,
+    ) -> None:
         self._bot = Bot(token=token)
         self._dispatcher = Dispatcher()
         self._router = Router()
         self._dispatcher.include_router(self._router)
+
         self._approval_chat_id = approval_chat_id
         self._approver_user_id = approver_user_id
         self._max_age_seconds = max_age_seconds
         self._store = store
         self._executor = executor
 
-        self._router.callback_query(IntentAction.filter(F.action == "execute"))(self._execute)
-        self._router.callback_query(IntentAction.filter(F.action == "skip"))(self._skip)
+        self._router.callback_query(IntentAction.filter(F.action == "execute"))(
+            self._execute
+        )
+        self._router.callback_query(IntentAction.filter(F.action == "skip"))(
+            self._skip
+        )
 
     async def start(self) -> None:
         await self._bot.delete_webhook(drop_pending_updates=False)
@@ -47,18 +60,29 @@ class ApprovalBot:
         await self._bot.session.close()
 
     async def send_intent(self, intent: TradingIntent) -> None:
-        keyboard = InlineKeyboardMarkup(inline_keyboard=[[
-            InlineKeyboardButton(
-                text="Execute",
-                callback_data=IntentAction(action="execute", intent_id=str(intent.intent_id)).pack(),
-                style="success",
-            ),
-            InlineKeyboardButton(
-                text="Skip",
-                callback_data=IntentAction(action="skip", intent_id=str(intent.intent_id)).pack(),
-                style="danger",
-            ),
-        ]])
+        keyboard = InlineKeyboardMarkup(
+            inline_keyboard=[
+                [
+                    InlineKeyboardButton(
+                        text="Execute",
+                        callback_data=IntentAction(
+                            action="execute",
+                            intent_id=str(intent.intent_id),
+                        ).pack(),
+                        style="success",
+                    ),
+                    InlineKeyboardButton(
+                        text="Skip",
+                        callback_data=IntentAction(
+                            action="skip",
+                            intent_id=str(intent.intent_id),
+                        ).pack(),
+                        style="danger",
+                    ),
+                ]
+            ]
+        )
+
         await self._bot.send_message(
             chat_id=self._approval_chat_id,
             text=self._render(intent),
@@ -67,61 +91,92 @@ class ApprovalBot:
             reply_markup=keyboard,
         )
 
-    async def _execute(self, callback: CallbackQuery, callback_data: IntentAction) -> None:
+    async def _execute(
+        self,
+        callback: CallbackQuery,
+        callback_data: IntentAction,
+    ) -> None:
         if callback.from_user.id != self._approver_user_id:
             await callback.answer("Not authorized", show_alert=True)
             return
-        await callback.answer()
 
         intent_id = UUID(callback_data.intent_id)
         intent = await self._store.get_intent(intent_id)
+
         if intent is None:
             await callback.answer("Intent no longer exists", show_alert=True)
             return
         if intent.status is not IntentStatus.PENDING:
-            await callback.answer(f"Already {intent.status.value.lower()}", show_alert=True)
+            await callback.answer(
+                f"Already {intent.status.value.lower()}",
+                show_alert=True,
+            )
             return
 
         age = (datetime.now(timezone.utc) - intent.created_at).total_seconds()
         if age > self._max_age_seconds:
-            await callback.answer(f"Intent is stale ({int(age)}s). Not executed.", show_alert=True)
+            await callback.answer(
+                f"Intent is stale ({int(age)}s). Not executed.",
+                show_alert=True,
+            )
             return
 
         if not await self._store.claim_for_execution(intent_id, callback.from_user.id):
             await callback.answer("Intent was already handled", show_alert=True)
             return
 
+        await callback.answer("Executing on Bybit Demo…")
+
         try:
             order_id = await self._executor.execute(intent)
         except Exception as exc:
             logger.exception("Execution failed for %s", intent_id)
             await self._store.mark_failed(intent_id, str(exc))
-            await self._edit_card(callback, intent, f"\n\n<b>FAILED</b>\n<code>{html.escape(str(exc))}</code>")
+            await self._edit_card(
+                callback,
+                intent,
+                f"\n\n<b>FAILED</b>\n<code>{html.escape(str(exc))}</code>",
+            )
             return
 
         await self._store.mark_executed(intent_id, order_id)
         await self._edit_card(
-            callback, intent,
-            f"\n\n<b>EXECUTED ON BYBIT DEMO</b>\nOrder ID: <code>{html.escape(order_id)}</code>"
+            callback,
+            intent,
+            (
+                "\n\n<b>EXECUTED ON BYBIT DEMO</b>"
+                f"\nOrder ID: <code>{html.escape(order_id)}</code>"
+            ),
         )
 
-    async def _skip(self, callback: CallbackQuery, callback_data: IntentAction) -> None:
+    async def _skip(
+        self,
+        callback: CallbackQuery,
+        callback_data: IntentAction,
+    ) -> None:
         if callback.from_user.id != self._approver_user_id:
             await callback.answer("Not authorized", show_alert=True)
             return
-        await callback.answer()
 
         intent_id = UUID(callback_data.intent_id)
         intent = await self._store.get_intent(intent_id)
+
         if intent is None:
             await callback.answer("Intent no longer exists", show_alert=True)
             return
         if not await self._store.mark_skipped(intent_id, callback.from_user.id):
             await callback.answer("Intent was already handled", show_alert=True)
             return
+
+        await callback.answer("Skipped")
         await self._edit_card(callback, intent, "\n\n<b>SKIPPED</b>")
 
-    async def _edit_card(self, callback: CallbackQuery, intent: TradingIntent, suffix: str) -> None:
+    async def _edit_card(
+        self,
+        callback: CallbackQuery,
+        intent: TradingIntent,
+        suffix: str,
+    ) -> None:
         if callback.message is not None:
             await callback.message.edit_text(
                 self._render(intent) + suffix,
@@ -135,19 +190,25 @@ class ApprovalBot:
         source_url = intent.source.telegram_url
         source_line = (
             f'<a href="{html.escape(source_url)}">Open source message</a>'
-            if source_url else "Source link unavailable"
+            if source_url
+            else "Source link unavailable"
+        )
+        entry = (
+            "Market"
+            if intent.entry.type is EntryType.MARKET
+            else f"{intent.entry.price:g}"
         )
-        entry = "Market" if intent.entry.type is EntryType.MARKET else f"{intent.entry.price:g}"
 
         risk_block = ""
         if intent.entry.price is not None:
-            e = intent.entry.price
+            entry_price = intent.entry.price
             if intent.side.value == "LONG":
-                risk = (e - intent.stop_loss) / e * 100
-                reward = (intent.take_profit - e) / e * 100
+                risk = (entry_price - intent.stop_loss) / entry_price * 100
+                reward = (intent.take_profit - entry_price) / entry_price * 100
             else:
-                risk = (intent.stop_loss - e) / e * 100
-                reward = (e - intent.take_profit) / e * 100
+                risk = (intent.stop_loss - entry_price) / entry_price * 100
+                reward = (entry_price - intent.take_profit) / entry_price * 100
+
             rr = reward / risk if risk > 0 else 0
             risk_block = (
                 f"\nRisk to SL: <b>{risk:.2f}%</b>"
@@ -166,4 +227,4 @@ class ApprovalBot:
             f"Trader: {html.escape(intent.source.channel_title)}\n"
             f"Published: {html.escape(intent.source.published_at.isoformat())}\n"
             f"{source_line}"
-        )
\ No newline at end of file
+        )
diff --git a/src/cautious_crypto_bro/bybit.py b/src/cautious_crypto_bro/bybit.py
index a5acdf1..1f346a5 100644
--- a/src/cautious_crypto_bro/bybit.py
+++ b/src/cautious_crypto_bro/bybit.py
@@ -13,8 +13,19 @@ class TradeExecutionError(RuntimeError):
 
 
 class BybitDemoExecutor:
-    def __init__(self, *, api_key: str, api_secret: str, notional_usdt: float) -> None:
-        self._session = HTTP(testnet=False, demo=True, api_key=api_key, api_secret=api_secret)
+    def __init__(
+        self,
+        *,
+        api_key: str,
+        api_secret: str,
+        notional_usdt: float,
+    ) -> None:
+        self._session = HTTP(
+            testnet=False,
+            demo=True,
+            api_key=api_key,
+            api_secret=api_secret,
+        )
         self._notional_usdt = Decimal(str(notional_usdt))
 
     async def execute(self, intent: TradingIntent) -> str:
@@ -22,7 +33,15 @@ class BybitDemoExecutor:
 
     def _execute_sync(self, intent: TradingIntent) -> str:
         market_price = self._last_price(intent.symbol)
-        sizing_price = Decimal(str(intent.entry.price)) if intent.entry.type is EntryType.LIMIT else market_price
+        sizing_price = (
+            Decimal(str(intent.entry.price))
+            if intent.entry.type is EntryType.LIMIT
+            else market_price
+        )
+
+        if intent.entry.type is EntryType.MARKET:
+            self._validate_market_geometry(intent, market_price)
+
         qty = self._quantity_for_notional(intent.symbol, sizing_price)
 
         params: dict[str, object] = {
@@ -43,10 +62,14 @@ class BybitDemoExecutor:
 
         response = self._session.place_order(**params)
         if response.get("retCode") != 0:
-            raise TradeExecutionError(f"Bybit rejected order: {response.get('retCode')} {response.get('retMsg')}")
+            raise TradeExecutionError(
+                f"Bybit rejected order: {response.get('retCode')} {response.get('retMsg')}"
+            )
+
         order_id = response.get("result", {}).get("orderId")
         if not order_id:
             raise TradeExecutionError("Bybit returned success without orderId")
+
         return str(order_id)
 
     def _last_price(self, symbol: str) -> Decimal:
@@ -57,7 +80,10 @@ class BybitDemoExecutor:
         return Decimal(items[0]["lastPrice"])
 
     def _quantity_for_notional(self, symbol: str, price: Decimal) -> Decimal:
-        response = self._session.get_instruments_info(category="linear", symbol=symbol)
+        response = self._session.get_instruments_info(
+            category="linear",
+            symbol=symbol,
+        )
         items = response.get("result", {}).get("list", [])
         if not items:
             raise TradeExecutionError(f"No Bybit instrument found for {symbol}")
@@ -69,12 +95,36 @@ class BybitDemoExecutor:
 
         raw = self._notional_usdt / price
         qty = (raw / step).to_integral_value(rounding=ROUND_DOWN) * step
+
         if qty < min_qty:
-            raise TradeExecutionError(f"Configured notional too small: qty {qty} < minOrderQty {min_qty}")
+            raise TradeExecutionError(
+                f"Configured notional too small: qty {qty} < minOrderQty {min_qty}"
+            )
         if min_notional and qty * price < min_notional:
-            raise TradeExecutionError(f"Configured notional too small: {qty * price} < minNotionalValue {min_notional}")
+            raise TradeExecutionError(
+                f"Configured notional too small: {qty * price} < "
+                f"minNotionalValue {min_notional}"
+            )
+
         return qty
 
+    @staticmethod
+    def _validate_market_geometry(
+        intent: TradingIntent,
+        market_price: Decimal,
+    ) -> None:
+        stop_loss = Decimal(str(intent.stop_loss))
+        take_profit = Decimal(str(intent.take_profit))
+
+        if intent.side is Side.LONG and not stop_loss < market_price < take_profit:
+            raise TradeExecutionError(
+                "Market price is outside LONG stop/target geometry"
+            )
+        if intent.side is Side.SHORT and not take_profit < market_price < stop_loss:
+            raise TradeExecutionError(
+                "Market price is outside SHORT stop/target geometry"
+            )
+
     @staticmethod
     def _fmt(value: Decimal) -> str:
-        return format(value.normalize(), "f")
\ No newline at end of file
+        return format(value.normalize(), "f")
diff --git a/src/cautious_crypto_bro/config.py b/src/cautious_crypto_bro/config.py
index 2a0e707..e61bcc7 100644
--- a/src/cautious_crypto_bro/config.py
+++ b/src/cautious_crypto_bro/config.py
@@ -3,17 +3,21 @@ from __future__ import annotations
 from functools import lru_cache
 from pathlib import Path
 
-from pydantic import field_validator
+from pydantic import Field
 from pydantic_settings import BaseSettings, SettingsConfigDict
 
 
 class Settings(BaseSettings):
-    model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", extra="ignore")
+    model_config = SettingsConfigDict(
+        env_file=".env",
+        env_file_encoding="utf-8",
+        extra="ignore",
+    )
 
     telegram_api_id: int
     telegram_api_hash: str
     telegram_session_name: str = "cautious_crypto_bro"
-    telegram_source_channels: list[str | int]
+    telegram_source_channels: list[str | int] = Field(min_length=1)
 
     telegram_bot_token: str
     telegram_approver_user_id: int
@@ -25,50 +29,13 @@ class Settings(BaseSettings):
 
     bybit_api_key: str
     bybit_api_secret: str
-    bybit_default_notional_usdt: float = 25.0
+    bybit_default_notional_usdt: float = Field(default=25.0, gt=0)
 
     database_path: Path = Path("data/cautious_crypto_bro.sqlite3")
-    intent_max_age_seconds: int = 900
+    intent_max_age_seconds: int = Field(default=900, gt=0)
     log_level: str = "INFO"
 
-    @field_validator("telegram_source_channels", mode="before")
-    @classmethod
-    def parse_channels(cls, value: object) -> object:
-        if isinstance(value, str):
-            channels: list[str | int] = []
-
-            for raw in value.split(","):
-                item = raw.strip()
-                if not item:
-                    continue
-
-                if item.lstrip("-").isdigit():
-                    channels.append(int(item))
-                else:
-                    channels.append(item.lstrip("@"))
-
-            return channels
-
-        return value
-
-    @field_validator("telegram_source_channels")
-    @classmethod
-    def require_channels(
-        cls,
-        value: list[str | int],
-    ) -> list[str | int]:
-        if not value:
-            raise ValueError("TELEGRAM_SOURCE_CHANNELS must contain at least one channel")
-        return value
-
-    @field_validator("bybit_default_notional_usdt")
-    @classmethod
-    def positive_notional(cls, value: float) -> float:
-        if value <= 0:
-            raise ValueError("BYBIT_DEFAULT_NOTIONAL_USDT must be > 0")
-        return value
-
 
 @lru_cache
 def get_settings() -> Settings:
-    return Settings()
\ No newline at end of file
+    return Settings()
diff --git a/src/cautious_crypto_bro/domain.py b/src/cautious_crypto_bro/domain.py
index f3ee8ca..dbd27bc 100644
--- a/src/cautious_crypto_bro/domain.py
+++ b/src/cautious_crypto_bro/domain.py
@@ -27,6 +27,7 @@ class IntentStatus(StrEnum):
 
 class SourceMessage(BaseModel):
     model_config = ConfigDict(extra="forbid")
+
     channel_id: int
     channel_title: str
     channel_username: str | None = None
@@ -39,11 +40,17 @@ class SourceMessage(BaseModel):
     def telegram_url(self) -> str | None:
         if self.channel_username:
             return f"https://t.me/{self.channel_username.lstrip('@')}/{self.message_id}"
+
+        channel_id = str(self.channel_id)
+        if channel_id.startswith("-100"):
+            return f"https://t.me/c/{channel_id[4:]}/{self.message_id}"
+
         return None
 
 
 class Entry(BaseModel):
     model_config = ConfigDict(extra="forbid")
+
     type: EntryType
     price: float | None = Field(default=None, gt=0)
 
@@ -58,6 +65,7 @@ class Entry(BaseModel):
 
 class ExtractedIntent(BaseModel):
     model_config = ConfigDict(extra="forbid")
+
     symbol: str
     side: Side
     entry: Entry
@@ -69,6 +77,7 @@ class ExtractedIntent(BaseModel):
 
 class IntentExtraction(BaseModel):
     model_config = ConfigDict(extra="forbid")
+
     actionable: bool
     reason: str = Field(min_length=1, max_length=500)
     intent: ExtractedIntent | None = None
@@ -99,9 +108,8 @@ class TradingIntent(BaseModel):
 
     @model_validator(mode="after")
     def validate_trade_geometry(self) -> "TradingIntent":
-        normalized = self.symbol.upper().replace("/", "").replace("-", "")
-        object.__setattr__(self, "symbol", normalized)
-        if not normalized.endswith("USDT"):
+        self.symbol = self.symbol.upper().replace("/", "").replace("-", "")
+        if not self.symbol.endswith("USDT"):
             raise ValueError("MVP supports only USDT linear symbols")
 
         reference = self.entry.price
@@ -115,4 +123,4 @@ class TradingIntent(BaseModel):
                 raise ValueError("LONG requires stop_loss < take_profit")
             if self.side is Side.SHORT and not self.take_profit < self.stop_loss:
                 raise ValueError("SHORT requires take_profit < stop_loss")
-        return self
\ No newline at end of file
+        return self
diff --git a/tests/test_domain.py b/tests/test_domain.py
index 83d40ca..d91ae04 100644
--- a/tests/test_domain.py
+++ b/tests/test_domain.py
@@ -44,4 +44,15 @@ def test_limit_requires_price() -> None:
 
 def test_market_rejects_price() -> None:
     with pytest.raises(ValidationError):
-        Entry(type=EntryType.MARKET, price=100)
\ No newline at end of file
+        Entry(type=EntryType.MARKET, price=100)
+
+def test_private_channel_url() -> None:
+    private_source = source().model_copy(
+        update={
+            "channel_id": -1002132062264,
+            "channel_username": None,
+            "message_id": 11482,
+        }
+    )
+
+    assert private_source.telegram_url == "https://t.me/c/2132062264/11482"
</file>

<file path="docker-compose.yaml">
services:
  app:
    build:
      context: .
      target: runtime
    env_file:
      - .env
    environment:
      # Keep mutable application state out of the source tree.
      TELEGRAM_SESSION_NAME: /state/cautious_crypto_bro
      DATABASE_PATH: /state/cautious_crypto_bro.sqlite3
    volumes:
      - app_state:/state
    stdin_open: true
    tty: true
    restart: unless-stopped

  test:
    build:
      context: .
      target: test
    profiles: ["test"]
    command: ["/app/.venv/bin/pytest", "-v"]

volumes:
  app_state:
</file>

<file path="Dockerfile">
FROM ghcr.io/astral-sh/uv:python3.12-bookworm-slim AS runtime

WORKDIR /app

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    UV_COMPILE_BYTECODE=1 \
    UV_LINK_MODE=copy

# Install third-party dependencies first so this layer remains cached when
# application code changes.
COPY pyproject.toml uv.lock ./
RUN uv sync --frozen --no-dev --no-install-project

COPY src ./src
COPY scripts ./scripts
RUN uv sync --frozen --no-dev

CMD ["/app/.venv/bin/cautious-crypto-bro"]


FROM runtime AS test

COPY tests ./tests
RUN uv sync --frozen --group dev

CMD ["/app/.venv/bin/pytest", "-v"]
</file>

<file path="pyproject.toml">
[project]
name = "cautious-crypto-bro"
version = "0.1.0"
description = "Human-in-the-loop Telegram crypto signal interpreter and Bybit demo executor"
requires-python = ">=3.12"
dependencies = [
  "aiogram>=3.31,<4",
  "aiosqlite>=0.21,<1",
  "httpx>=0.28,<1",
  "pydantic>=2.11,<3",
  "pydantic-settings>=2.10,<3",
  "pybit>=5.14,<6",
  "telethon>=1.40,<2",
]

[dependency-groups]
dev = [
  "pytest>=8.4,<9",
  "pytest-asyncio>=1.1,<2",
]

[project.scripts]
cautious-crypto-bro = "cautious_crypto_bro.main:main"

[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cautious_crypto_bro"]

[tool.pytest.ini_options]
pythonpath = ["src"]
asyncio_mode = "auto"
testpaths = ["tests"]
</file>

<file path="README.md">
# Cautious Crypto Bro

Weekend MVP: watch selected Telegram trader channels, turn live text/captions into structured trading intents, show those intents in an approval Telegram bot, and place a Bybit **Demo Trading** order only after explicit approval.

## MVP scope

Implemented:

- live Telegram channel ingestion via a dedicated Telegram user session
- text/caption signal interpretation through OpenRouter
- strict Pydantic `TradingIntent` validation
- Telegram approval cards with `Execute` / `Skip`
- only the configured Telegram user can approve trades
- Bybit Demo Trading execution
- fixed configurable USDT notional sizing
- quantity rounding to Bybit instrument rules
- SL/TP passed with the order
- SQLite persistence for source messages, intents, decisions, and execution result
- stale-intent check before execution
- idempotent state transition so the same intent cannot be executed twice

Deliberately not implemented yet:

- image / voice / video understanding
- multi-message idea-thread reconstruction
- edits to an existing trade
- multiple take-profits / scaling
- live position monitoring and PnL reporting
- historical backtesting

## Runtime model

The application is Docker-first. You do not need a project-local Python installation or `.venv`.

- `uv` resolves and installs Python dependencies inside Docker.
- `uv.lock` is committed for reproducible builds.
- Telegram session state and SQLite live in the Docker named volume `app_state`.
- `.env` is the only local runtime configuration file and is gitignored.

## Initial setup

Create your local environment file:

```bash
cp .env.example .env
```

Fill in `.env` with Telegram, OpenRouter, and Bybit Demo credentials.

Generate the lock file using the uv Docker image, so uv does not need to be installed on the host:

```bash
docker run --rm \
  --user "$(id -u):$(id -g)" \
  -v "$PWD:/workspace" \
  -w /workspace \
  ghcr.io/astral-sh/uv:0.10.0-python3.12-bookworm-slim \
  uv lock
```

Build and test:

```bash
docker compose build
docker compose --profile test run --rm test
```

## First Telegram login

Telethon needs one interactive login for the dedicated Telegram account. Run:

```bash
docker compose run --rm app
```

Enter the Telegram phone/code/password when prompted. The resulting session is saved in the Docker named volume, so subsequent containers reuse it.

Stop the foreground process after authentication, then start normally:

```bash
docker compose up -d app
docker compose logs -f app
```

## Useful commands

Run tests:

```bash
docker compose --profile test run --rm test
```

Rebuild after dependency or source changes:

```bash
docker compose build app
```

Run in foreground:

```bash
docker compose up app
```

Stop the application without deleting state:

```bash
docker compose down
```

Delete all local application state, including the Telegram session and SQLite database:

```bash
docker compose down -v
```

## Trading behavior

The LLM is not allowed to place orders. It may only emit a `TradingIntent`.

An intent must contain:

- `symbol`
- `side`
- `entry`
- `stop_loss`
- `take_profit`
- `summary`
- confidence

If the model decides the post is not an actionable trade, or cannot identify required trade parameters without inventing them, no approval card is created.

`Execute` performs deterministic validation and then calls Bybit Demo Trading.

## Important

This repository intentionally defaults to Bybit Demo Trading through `pybit(..., demo=True)`. Do not change this to live trading until the execution and validation layers have been deliberately redesigned and reviewed.
</file>

<file path="repomix-output.xml">
This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
.dockerignore
.env.example
.gitignore
docker-compose.yaml
Dockerfile
pyproject.toml
README.md
scripts/list_telegram_dialogs.py
src/cautious_crypto_bro/__init__.py
src/cautious_crypto_bro/approval_bot.py
src/cautious_crypto_bro/bybit.py
src/cautious_crypto_bro/config.py
src/cautious_crypto_bro/domain.py
src/cautious_crypto_bro/main.py
src/cautious_crypto_bro/openrouter.py
src/cautious_crypto_bro/service.py
src/cautious_crypto_bro/storage.py
src/cautious_crypto_bro/telegram_source.py
tests/test_approval_render.py
tests/test_domain.py
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path=".dockerignore">
.git
.gitignore
.env
.venv
__pycache__
*.py[cod]
.pytest_cache
*.session
*.session-journal
data
build
dist
*.egg-info
cautious-crypto-bro-architecture.svg
</file>

<file path=".env.example">
TELEGRAM_API_ID=
TELEGRAM_API_HASH=
TELEGRAM_SESSION_NAME=cautious_crypto_bro
TELEGRAM_SOURCE_CHANNELS=

TELEGRAM_BOT_TOKEN=
TELEGRAM_APPROVER_USER_ID=
TELEGRAM_APPROVAL_CHAT_ID=

OPENROUTER_API_KEY=
OPENROUTER_MODEL=google/gemma-4-26b-a4b-it
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

BYBIT_API_KEY=
BYBIT_API_SECRET=
BYBIT_DEFAULT_NOTIONAL_USDT=25

DATABASE_PATH=data/cautious_crypto_bro.sqlite3
INTENT_MAX_AGE_SECONDS=900
LOG_LEVEL=INFO
</file>

<file path=".gitignore">
.env
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
*.session
*.session-journal
data/
build/
dist/
*.egg-info/
</file>

<file path="docker-compose.yaml">
services:
  app:
    build:
      context: .
      target: runtime
    env_file:
      - .env
    environment:
      # Keep mutable application state out of the source tree.
      TELEGRAM_SESSION_NAME: /state/cautious_crypto_bro
      DATABASE_PATH: /state/cautious_crypto_bro.sqlite3
    volumes:
      - app_state:/state
    stdin_open: true
    tty: true
    restart: unless-stopped

  test:
    build:
      context: .
      target: test
    profiles: ["test"]
    command: ["/app/.venv/bin/pytest", "-v"]

volumes:
  app_state:
</file>

<file path="Dockerfile">
FROM ghcr.io/astral-sh/uv:python3.12-bookworm-slim AS runtime

WORKDIR /app

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    UV_COMPILE_BYTECODE=1 \
    UV_LINK_MODE=copy

# Install third-party dependencies first so this layer remains cached when
# application code changes.
COPY pyproject.toml uv.lock ./
RUN uv sync --frozen --no-dev --no-install-project

COPY src ./src
COPY scripts ./scripts
RUN uv sync --frozen --no-dev

CMD ["/app/.venv/bin/cautious-crypto-bro"]


FROM runtime AS test

COPY tests ./tests
RUN uv sync --frozen --group dev

CMD ["/app/.venv/bin/pytest", "-v"]
</file>

<file path="pyproject.toml">
[project]
name = "cautious-crypto-bro"
version = "0.1.0"
description = "Human-in-the-loop Telegram crypto signal interpreter and Bybit demo executor"
requires-python = ">=3.12"
dependencies = [
  "aiogram>=3.31,<4",
  "aiosqlite>=0.21,<1",
  "httpx>=0.28,<1",
  "pydantic>=2.11,<3",
  "pydantic-settings>=2.10,<3",
  "pybit>=5.14,<6",
  "telethon>=1.40,<2",
]

[dependency-groups]
dev = [
  "pytest>=8.4,<9",
  "pytest-asyncio>=1.1,<2",
]

[project.scripts]
cautious-crypto-bro = "cautious_crypto_bro.main:main"

[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cautious_crypto_bro"]

[tool.pytest.ini_options]
pythonpath = ["src"]
asyncio_mode = "auto"
testpaths = ["tests"]
</file>

<file path="README.md">
# Cautious Crypto Bro

Weekend MVP: watch selected Telegram trader channels, turn live text/captions into structured trading intents, show those intents in an approval Telegram bot, and place a Bybit **Demo Trading** order only after explicit approval.

## MVP scope

Implemented:

- live Telegram channel ingestion via a dedicated Telegram user session
- text/caption signal interpretation through OpenRouter
- strict Pydantic `TradingIntent` validation
- Telegram approval cards with `Execute` / `Skip`
- only the configured Telegram user can approve trades
- Bybit Demo Trading execution
- fixed configurable USDT notional sizing
- quantity rounding to Bybit instrument rules
- SL/TP passed with the order
- SQLite persistence for source messages, intents, decisions, and execution result
- stale-intent check before execution
- idempotent state transition so the same intent cannot be executed twice

Deliberately not implemented yet:

- image / voice / video understanding
- multi-message idea-thread reconstruction
- edits to an existing trade
- multiple take-profits / scaling
- live position monitoring and PnL reporting
- historical backtesting

## Runtime model

The application is Docker-first. You do not need a project-local Python installation or `.venv`.

- `uv` resolves and installs Python dependencies inside Docker.
- `uv.lock` is committed for reproducible builds.
- Telegram session state and SQLite live in the Docker named volume `app_state`.
- `.env` is the only local runtime configuration file and is gitignored.

## Initial setup

Create your local environment file:

```bash
cp .env.example .env
```

Fill in `.env` with Telegram, OpenRouter, and Bybit Demo credentials.

Generate the lock file using the uv Docker image, so uv does not need to be installed on the host:

```bash
docker run --rm \
  --user "$(id -u):$(id -g)" \
  -v "$PWD:/workspace" \
  -w /workspace \
  ghcr.io/astral-sh/uv:0.10.0-python3.12-bookworm-slim \
  uv lock
```

Build and test:

```bash
docker compose build
docker compose --profile test run --rm test
```

## First Telegram login

Telethon needs one interactive login for the dedicated Telegram account. Run:

```bash
docker compose run --rm app
```

Enter the Telegram phone/code/password when prompted. The resulting session is saved in the Docker named volume, so subsequent containers reuse it.

Stop the foreground process after authentication, then start normally:

```bash
docker compose up -d app
docker compose logs -f app
```

## Useful commands

Run tests:

```bash
docker compose --profile test run --rm test
```

Rebuild after dependency or source changes:

```bash
docker compose build app
```

Run in foreground:

```bash
docker compose up app
```

Stop the application without deleting state:

```bash
docker compose down
```

Delete all local application state, including the Telegram session and SQLite database:

```bash
docker compose down -v
```

## Trading behavior

The LLM is not allowed to place orders. It may only emit a `TradingIntent`.

An intent must contain:

- `symbol`
- `side`
- `entry`
- `stop_loss`
- `take_profit`
- `summary`
- confidence

If the model decides the post is not an actionable trade, or cannot identify required trade parameters without inventing them, no approval card is created.

`Execute` performs deterministic validation and then calls Bybit Demo Trading.

## Important

This repository intentionally defaults to Bybit Demo Trading through `pybit(..., demo=True)`. Do not change this to live trading until the execution and validation layers have been deliberately redesigned and reviewed.
</file>

<file path="scripts/list_telegram_dialogs.py">
import asyncio
import os

from telethon import TelegramClient


async def main() -> None:
    api_id = int(os.environ["TELEGRAM_API_ID"])
    api_hash = os.environ["TELEGRAM_API_HASH"]
    session_name = os.environ.get(
        "TELEGRAM_SESSION_NAME",
        "/state/cautious_crypto_bro",
    )

    client = TelegramClient(
        session_name,
        api_id,
        api_hash,
    )

    await client.start()

    print()
    print(
        f"{'ID':>18}  "
        f"{'TYPE':<16}  "
        f"{'USERNAME':<30}  "
        f"NAME"
    )
    print("-" * 100)

    async for dialog in client.iter_dialogs():
        entity = dialog.entity
        username = getattr(entity, "username", None)

        print(
            f"{dialog.id:>18}  "
            f"{type(entity).__name__:<16}  "
            f"{('@' + username) if username else '-':<30}  "
            f"{dialog.name}"
        )

    await client.disconnect()


if __name__ == "__main__":
    asyncio.run(main())
</file>

<file path="src/cautious_crypto_bro/__init__.py">
__all__ = []
</file>

<file path="src/cautious_crypto_bro/approval_bot.py">
from __future__ import annotations

import html
import logging
from datetime import datetime, timezone
from uuid import UUID

from aiogram import Bot, Dispatcher, F, Router
from aiogram.filters.callback_data import CallbackData
from aiogram.types import CallbackQuery, InlineKeyboardButton, InlineKeyboardMarkup

from .bybit import BybitDemoExecutor
from .domain import EntryType, IntentStatus, TradingIntent
from .storage import IntentStore

logger = logging.getLogger(__name__)


class IntentAction(CallbackData, prefix="intent"):
    action: str
    intent_id: str


class ApprovalBot:
    def __init__(self, *, token: str, approval_chat_id: int, approver_user_id: int,
                 max_age_seconds: int, store: IntentStore, executor: BybitDemoExecutor) -> None:
        self._bot = Bot(token=token)
        self._dispatcher = Dispatcher()
        self._router = Router()
        self._dispatcher.include_router(self._router)
        self._approval_chat_id = approval_chat_id
        self._approver_user_id = approver_user_id
        self._max_age_seconds = max_age_seconds
        self._store = store
        self._executor = executor

        self._router.callback_query(IntentAction.filter(F.action == "execute"))(self._execute)
        self._router.callback_query(IntentAction.filter(F.action == "skip"))(self._skip)

    async def start(self) -> None:
        await self._bot.delete_webhook(drop_pending_updates=False)

    async def run(self) -> None:
        await self._dispatcher.start_polling(self._bot)

    async def close(self) -> None:
        await self._bot.session.close()

    async def send_intent(self, intent: TradingIntent) -> None:
        keyboard = InlineKeyboardMarkup(inline_keyboard=[[
            InlineKeyboardButton(
                text="Execute",
                callback_data=IntentAction(action="execute", intent_id=str(intent.intent_id)).pack(),
                style="success",
            ),
            InlineKeyboardButton(
                text="Skip",
                callback_data=IntentAction(action="skip", intent_id=str(intent.intent_id)).pack(),
                style="danger",
            ),
        ]])
        await self._bot.send_message(
            chat_id=self._approval_chat_id,
            text=self._render(intent),
            parse_mode="HTML",
            disable_web_page_preview=True,
            reply_markup=keyboard,
        )

    async def _execute(self, callback: CallbackQuery, callback_data: IntentAction) -> None:
        if callback.from_user.id != self._approver_user_id:
            await callback.answer("Not authorized", show_alert=True)
            return
        await callback.answer()

        intent_id = UUID(callback_data.intent_id)
        intent = await self._store.get_intent(intent_id)
        if intent is None:
            await callback.answer("Intent no longer exists", show_alert=True)
            return
        if intent.status is not IntentStatus.PENDING:
            await callback.answer(f"Already {intent.status.value.lower()}", show_alert=True)
            return

        age = (datetime.now(timezone.utc) - intent.created_at).total_seconds()
        if age > self._max_age_seconds:
            await callback.answer(f"Intent is stale ({int(age)}s). Not executed.", show_alert=True)
            return

        if not await self._store.claim_for_execution(intent_id, callback.from_user.id):
            await callback.answer("Intent was already handled", show_alert=True)
            return

        try:
            order_id = await self._executor.execute(intent)
        except Exception as exc:
            logger.exception("Execution failed for %s", intent_id)
            await self._store.mark_failed(intent_id, str(exc))
            await self._edit_card(callback, intent, f"\n\n<b>FAILED</b>\n<code>{html.escape(str(exc))}</code>")
            return

        await self._store.mark_executed(intent_id, order_id)
        await self._edit_card(
            callback, intent,
            f"\n\n<b>EXECUTED ON BYBIT DEMO</b>\nOrder ID: <code>{html.escape(order_id)}</code>"
        )

    async def _skip(self, callback: CallbackQuery, callback_data: IntentAction) -> None:
        if callback.from_user.id != self._approver_user_id:
            await callback.answer("Not authorized", show_alert=True)
            return
        await callback.answer()

        intent_id = UUID(callback_data.intent_id)
        intent = await self._store.get_intent(intent_id)
        if intent is None:
            await callback.answer("Intent no longer exists", show_alert=True)
            return
        if not await self._store.mark_skipped(intent_id, callback.from_user.id):
            await callback.answer("Intent was already handled", show_alert=True)
            return
        await self._edit_card(callback, intent, "\n\n<b>SKIPPED</b>")

    async def _edit_card(self, callback: CallbackQuery, intent: TradingIntent, suffix: str) -> None:
        if callback.message is not None:
            await callback.message.edit_text(
                self._render(intent) + suffix,
                parse_mode="HTML",
                disable_web_page_preview=True,
                reply_markup=None,
            )

    @staticmethod
    def _render(intent: TradingIntent) -> str:
        source_url = intent.source.telegram_url
        source_line = (
            f'<a href="{html.escape(source_url)}">Open source message</a>'
            if source_url else "Source link unavailable"
        )
        entry = "Market" if intent.entry.type is EntryType.MARKET else f"{intent.entry.price:g}"

        risk_block = ""
        if intent.entry.price is not None:
            e = intent.entry.price
            if intent.side.value == "LONG":
                risk = (e - intent.stop_loss) / e * 100
                reward = (intent.take_profit - e) / e * 100
            else:
                risk = (intent.stop_loss - e) / e * 100
                reward = (e - intent.take_profit) / e * 100
            rr = reward / risk if risk > 0 else 0
            risk_block = (
                f"\nRisk to SL: <b>{risk:.2f}%</b>"
                f"\nReward to TP: <b>{reward:.2f}%</b>"
                f"\nR:R: <b>{rr:.2f}</b>"
            )

        return (
            f"<b>{html.escape(intent.side.value)} {html.escape(intent.symbol)}</b>\n\n"
            f"Entry: <b>{html.escape(entry)}</b>\n"
            f"Stop loss: <b>{intent.stop_loss:g}</b>\n"
            f"Take profit: <b>{intent.take_profit:g}</b>"
            f"{risk_block}\n\n"
            f"Confidence: <b>{intent.confidence:.0%}</b>\n"
            f"Thesis: {html.escape(intent.summary)}\n\n"
            f"Trader: {html.escape(intent.source.channel_title)}\n"
            f"Published: {html.escape(intent.source.published_at.isoformat())}\n"
            f"{source_line}"
        )
</file>

<file path="src/cautious_crypto_bro/bybit.py">
from __future__ import annotations

import asyncio
from decimal import Decimal, ROUND_DOWN

from pybit.unified_trading import HTTP

from .domain import EntryType, Side, TradingIntent


class TradeExecutionError(RuntimeError):
    pass


class BybitDemoExecutor:
    def __init__(self, *, api_key: str, api_secret: str, notional_usdt: float) -> None:
        self._session = HTTP(testnet=False, demo=True, api_key=api_key, api_secret=api_secret)
        self._notional_usdt = Decimal(str(notional_usdt))

    async def execute(self, intent: TradingIntent) -> str:
        return await asyncio.to_thread(self._execute_sync, intent)

    def _execute_sync(self, intent: TradingIntent) -> str:
        market_price = self._last_price(intent.symbol)
        sizing_price = Decimal(str(intent.entry.price)) if intent.entry.type is EntryType.LIMIT else market_price
        qty = self._quantity_for_notional(intent.symbol, sizing_price)

        params: dict[str, object] = {
            "category": "linear",
            "symbol": intent.symbol,
            "side": "Buy" if intent.side is Side.LONG else "Sell",
            "orderType": "Market" if intent.entry.type is EntryType.MARKET else "Limit",
            "qty": self._fmt(qty),
            "takeProfit": self._fmt(Decimal(str(intent.take_profit))),
            "stopLoss": self._fmt(Decimal(str(intent.stop_loss))),
            "tpslMode": "Full",
            "tpOrderType": "Market",
            "slOrderType": "Market",
            "timeInForce": "GTC",
        }
        if intent.entry.type is EntryType.LIMIT:
            params["price"] = self._fmt(Decimal(str(intent.entry.price)))

        response = self._session.place_order(**params)
        if response.get("retCode") != 0:
            raise TradeExecutionError(f"Bybit rejected order: {response.get('retCode')} {response.get('retMsg')}")
        order_id = response.get("result", {}).get("orderId")
        if not order_id:
            raise TradeExecutionError("Bybit returned success without orderId")
        return str(order_id)

    def _last_price(self, symbol: str) -> Decimal:
        response = self._session.get_tickers(category="linear", symbol=symbol)
        items = response.get("result", {}).get("list", [])
        if not items:
            raise TradeExecutionError(f"No Bybit ticker found for {symbol}")
        return Decimal(items[0]["lastPrice"])

    def _quantity_for_notional(self, symbol: str, price: Decimal) -> Decimal:
        response = self._session.get_instruments_info(category="linear", symbol=symbol)
        items = response.get("result", {}).get("list", [])
        if not items:
            raise TradeExecutionError(f"No Bybit instrument found for {symbol}")

        lot = items[0]["lotSizeFilter"]
        step = Decimal(lot["qtyStep"])
        min_qty = Decimal(lot["minOrderQty"])
        min_notional = Decimal(lot.get("minNotionalValue", "0"))

        raw = self._notional_usdt / price
        qty = (raw / step).to_integral_value(rounding=ROUND_DOWN) * step
        if qty < min_qty:
            raise TradeExecutionError(f"Configured notional too small: qty {qty} < minOrderQty {min_qty}")
        if min_notional and qty * price < min_notional:
            raise TradeExecutionError(f"Configured notional too small: {qty * price} < minNotionalValue {min_notional}")
        return qty

    @staticmethod
    def _fmt(value: Decimal) -> str:
        return format(value.normalize(), "f")
</file>

<file path="src/cautious_crypto_bro/config.py">
from __future__ import annotations

from functools import lru_cache
from pathlib import Path

from pydantic import field_validator
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", extra="ignore")

    telegram_api_id: int
    telegram_api_hash: str
    telegram_session_name: str = "cautious_crypto_bro"
    telegram_source_channels: list[str | int]

    telegram_bot_token: str
    telegram_approver_user_id: int
    telegram_approval_chat_id: int

    openrouter_api_key: str
    openrouter_model: str = "google/gemma-4-26b-a4b-it"
    openrouter_base_url: str = "https://openrouter.ai/api/v1"

    bybit_api_key: str
    bybit_api_secret: str
    bybit_default_notional_usdt: float = 25.0

    database_path: Path = Path("data/cautious_crypto_bro.sqlite3")
    intent_max_age_seconds: int = 900
    log_level: str = "INFO"

    @field_validator("telegram_source_channels", mode="before")
    @classmethod
    def parse_channels(cls, value: object) -> object:
        if isinstance(value, str):
            channels: list[str | int] = []

            for raw in value.split(","):
                item = raw.strip()
                if not item:
                    continue

                if item.lstrip("-").isdigit():
                    channels.append(int(item))
                else:
                    channels.append(item.lstrip("@"))

            return channels

        return value

    @field_validator("telegram_source_channels")
    @classmethod
    def require_channels(
        cls,
        value: list[str | int],
    ) -> list[str | int]:
        if not value:
            raise ValueError("TELEGRAM_SOURCE_CHANNELS must contain at least one channel")
        return value

    @field_validator("bybit_default_notional_usdt")
    @classmethod
    def positive_notional(cls, value: float) -> float:
        if value <= 0:
            raise ValueError("BYBIT_DEFAULT_NOTIONAL_USDT must be > 0")
        return value


@lru_cache
def get_settings() -> Settings:
    return Settings()
</file>

<file path="src/cautious_crypto_bro/domain.py">
from __future__ import annotations

from datetime import datetime, timezone
from enum import StrEnum
from uuid import UUID, uuid4

from pydantic import BaseModel, ConfigDict, Field, model_validator


class Side(StrEnum):
    LONG = "LONG"
    SHORT = "SHORT"


class EntryType(StrEnum):
    MARKET = "MARKET"
    LIMIT = "LIMIT"


class IntentStatus(StrEnum):
    PENDING = "PENDING"
    EXECUTING = "EXECUTING"
    EXECUTED = "EXECUTED"
    SKIPPED = "SKIPPED"
    FAILED = "FAILED"


class SourceMessage(BaseModel):
    model_config = ConfigDict(extra="forbid")
    channel_id: int
    channel_title: str
    channel_username: str | None = None
    message_id: int
    published_at: datetime
    received_at: datetime
    text: str

    @property
    def telegram_url(self) -> str | None:
        if self.channel_username:
            return f"https://t.me/{self.channel_username.lstrip('@')}/{self.message_id}"
        return None


class Entry(BaseModel):
    model_config = ConfigDict(extra="forbid")
    type: EntryType
    price: float | None = Field(default=None, gt=0)

    @model_validator(mode="after")
    def validate_price(self) -> "Entry":
        if self.type is EntryType.LIMIT and self.price is None:
            raise ValueError("LIMIT entry requires a price")
        if self.type is EntryType.MARKET and self.price is not None:
            raise ValueError("MARKET entry must not contain a price")
        return self


class ExtractedIntent(BaseModel):
    model_config = ConfigDict(extra="forbid")
    symbol: str
    side: Side
    entry: Entry
    stop_loss: float = Field(gt=0)
    take_profit: float = Field(gt=0)
    summary: str = Field(min_length=1, max_length=500)
    confidence: float = Field(ge=0, le=1)


class IntentExtraction(BaseModel):
    model_config = ConfigDict(extra="forbid")
    actionable: bool
    reason: str = Field(min_length=1, max_length=500)
    intent: ExtractedIntent | None = None

    @model_validator(mode="after")
    def consistent_actionability(self) -> "IntentExtraction":
        if self.actionable and self.intent is None:
            raise ValueError("actionable=true requires intent")
        if not self.actionable and self.intent is not None:
            raise ValueError("actionable=false requires intent=null")
        return self


class TradingIntent(BaseModel):
    model_config = ConfigDict(extra="forbid")

    intent_id: UUID = Field(default_factory=uuid4)
    source: SourceMessage
    symbol: str
    side: Side
    entry: Entry
    stop_loss: float = Field(gt=0)
    take_profit: float = Field(gt=0)
    summary: str = Field(min_length=1, max_length=500)
    confidence: float = Field(ge=0, le=1)
    created_at: datetime = Field(default_factory=lambda: datetime.now(timezone.utc))
    status: IntentStatus = IntentStatus.PENDING

    @model_validator(mode="after")
    def validate_trade_geometry(self) -> "TradingIntent":
        normalized = self.symbol.upper().replace("/", "").replace("-", "")
        object.__setattr__(self, "symbol", normalized)
        if not normalized.endswith("USDT"):
            raise ValueError("MVP supports only USDT linear symbols")

        reference = self.entry.price
        if reference is not None:
            if self.side is Side.LONG and not self.stop_loss < reference < self.take_profit:
                raise ValueError("LONG requires stop_loss < entry < take_profit")
            if self.side is Side.SHORT and not self.take_profit < reference < self.stop_loss:
                raise ValueError("SHORT requires take_profit < entry < stop_loss")
        else:
            if self.side is Side.LONG and not self.stop_loss < self.take_profit:
                raise ValueError("LONG requires stop_loss < take_profit")
            if self.side is Side.SHORT and not self.take_profit < self.stop_loss:
                raise ValueError("SHORT requires take_profit < stop_loss")
        return self
</file>

<file path="src/cautious_crypto_bro/main.py">
from __future__ import annotations

import asyncio
import logging

from .approval_bot import ApprovalBot
from .bybit import BybitDemoExecutor
from .config import get_settings
from .openrouter import OpenRouterIntentExtractor
from .service import SignalService
from .storage import IntentStore
from .telegram_source import TelegramSource


async def async_main() -> None:
    settings = get_settings()
    logging.basicConfig(
        level=getattr(logging, settings.log_level.upper(), logging.INFO),
        format="%(asctime)s %(levelname)s %(name)s: %(message)s",
    )

    store = IntentStore(settings.database_path)
    await store.initialize()

    extractor = OpenRouterIntentExtractor(
        api_key=settings.openrouter_api_key,
        model=settings.openrouter_model,
        base_url=settings.openrouter_base_url,
    )
    executor = BybitDemoExecutor(
        api_key=settings.bybit_api_key,
        api_secret=settings.bybit_api_secret,
        notional_usdt=settings.bybit_default_notional_usdt,
    )
    bot = ApprovalBot(
        token=settings.telegram_bot_token,
        approval_chat_id=settings.telegram_approval_chat_id,
        approver_user_id=settings.telegram_approver_user_id,
        max_age_seconds=settings.intent_max_age_seconds,
        store=store,
        executor=executor,
    )
    await bot.start()

    service = SignalService(store=store, extractor=extractor, approval_bot=bot)
    source = TelegramSource(
        api_id=settings.telegram_api_id,
        api_hash=settings.telegram_api_hash,
        session_name=settings.telegram_session_name,
        channels=settings.telegram_source_channels,
        on_message=service.on_message,
    )
    await source.start()

    try:
        async with asyncio.TaskGroup() as tg:
            tg.create_task(bot.run())
            tg.create_task(source.run_until_disconnected())
    finally:
        await source.disconnect()
        await bot.close()
        await extractor.close()


def main() -> None:
    asyncio.run(async_main())


if __name__ == "__main__":
    main()
</file>

<file path="src/cautious_crypto_bro/openrouter.py">
from __future__ import annotations

import logging
import httpx

from .domain import IntentExtraction, SourceMessage, TradingIntent

logger = logging.getLogger(__name__)

SYSTEM_PROMPT = """
You are a conservative crypto trading-signal parser.
Do not give trading advice and do not invent missing information.

Decide whether ONE Telegram post/caption contains one complete, immediately
actionable trade proposal representable by the supplied schema.

MVP rules:
- only USDT linear/perpetual-style symbols
- required: symbol, LONG/SHORT, entry semantics, stop loss, take profit
- MARKET only if the author clearly says enter now/at market or clearly states they entered now
- otherwise an explicit numeric entry price is required and entry.type must be LIMIT
- exactly one stop and one target
- never infer prices from vague phrases such as "below support", "previous low",
  "ATH", "the box", or information not present in the supplied text
- if the post is commentary, an update to an older idea, incomplete, ambiguous,
  or depends on unseen media, actionable=false
- confidence is extraction confidence, not probability of profit
- summary is one short sentence describing the trader's stated thesis
""".strip()


class OpenRouterIntentExtractor:
    def __init__(self, *, api_key: str, model: str, base_url: str) -> None:
        self._model = model
        self._client = httpx.AsyncClient(
            base_url=base_url.rstrip("/"),
            headers={"Authorization": f"Bearer {api_key}", "Content-Type": "application/json"},
            timeout=httpx.Timeout(45.0),
        )

    async def close(self) -> None:
        await self._client.aclose()

    async def extract(self, source: SourceMessage) -> TradingIntent | None:
        payload = {
            "model": self._model,
            "messages": [
                {"role": "system", "content": SYSTEM_PROMPT},
                {"role": "user", "content": (
                    f"Channel: {source.channel_title}\n"
                    f"Published: {source.published_at.isoformat()}\n\n"
                    f"Telegram post:\n{source.text}"
                )},
            ],
            "temperature": 0,
            "response_format": {
                "type": "json_schema",
                "json_schema": {
                    "name": "trading_intent_extraction",
                    "strict": True,
                    "schema": IntentExtraction.model_json_schema(),
                },
            },
        }
        response = await self._client.post("/chat/completions", json=payload)
        response.raise_for_status()
        content = response.json()["choices"][0]["message"]["content"]
        extraction = IntentExtraction.model_validate_json(content)

        if not extraction.actionable or extraction.intent is None:
            logger.info("No actionable intent for %s/%s: %s",
                        source.channel_id, source.message_id, extraction.reason)
            return None

        raw = extraction.intent
        return TradingIntent(
            source=source,
            symbol=raw.symbol,
            side=raw.side,
            entry=raw.entry,
            stop_loss=raw.stop_loss,
            take_profit=raw.take_profit,
            summary=raw.summary,
            confidence=raw.confidence,
        )
</file>

<file path="src/cautious_crypto_bro/service.py">
from __future__ import annotations

import logging

from .approval_bot import ApprovalBot
from .domain import SourceMessage
from .openrouter import OpenRouterIntentExtractor
from .storage import IntentStore

logger = logging.getLogger(__name__)


class SignalService:
    def __init__(self, *, store: IntentStore, extractor: OpenRouterIntentExtractor,
                 approval_bot: ApprovalBot) -> None:
        self._store = store
        self._extractor = extractor
        self._approval_bot = approval_bot

    async def on_message(self, source: SourceMessage) -> None:
        if not await self._store.save_source(source):
            logger.debug("Duplicate Telegram message %s/%s", source.channel_id, source.message_id)
            return
        try:
            intent = await self._extractor.extract(source)
        except Exception:
            logger.exception("Intent extraction failed for %s/%s", source.channel_id, source.message_id)
            return
        if intent is None:
            return
        await self._store.create_intent(intent)
        await self._approval_bot.send_intent(intent)
        logger.info("Created trading intent %s", intent.intent_id)
</file>

<file path="src/cautious_crypto_bro/storage.py">
from __future__ import annotations

from pathlib import Path
from uuid import UUID

import aiosqlite

from .domain import IntentStatus, SourceMessage, TradingIntent


class IntentStore:
    def __init__(self, database_path: Path) -> None:
        self._database_path = database_path

    async def initialize(self) -> None:
        self._database_path.parent.mkdir(parents=True, exist_ok=True)
        async with aiosqlite.connect(self._database_path) as db:
            await db.executescript(
                """
                PRAGMA journal_mode=WAL;
                CREATE TABLE IF NOT EXISTS source_messages (
                    channel_id INTEGER NOT NULL,
                    message_id INTEGER NOT NULL,
                    payload_json TEXT NOT NULL,
                    PRIMARY KEY (channel_id, message_id)
                );
                CREATE TABLE IF NOT EXISTS intents (
                    intent_id TEXT PRIMARY KEY,
                    channel_id INTEGER NOT NULL,
                    message_id INTEGER NOT NULL,
                    payload_json TEXT NOT NULL,
                    status TEXT NOT NULL,
                    decision_user_id INTEGER,
                    bybit_order_id TEXT,
                    error TEXT,
                    created_at TEXT NOT NULL,
                    updated_at TEXT NOT NULL
                );
                """
            )
            await db.commit()

    async def save_source(self, source: SourceMessage) -> bool:
        async with aiosqlite.connect(self._database_path) as db:
            cursor = await db.execute(
                """INSERT OR IGNORE INTO source_messages(channel_id, message_id, payload_json)
                   VALUES (?, ?, ?)""",
                (source.channel_id, source.message_id, source.model_dump_json()),
            )
            await db.commit()
            return cursor.rowcount == 1

    async def create_intent(self, intent: TradingIntent) -> None:
        now = intent.created_at.isoformat()
        async with aiosqlite.connect(self._database_path) as db:
            await db.execute(
                """INSERT INTO intents(
                       intent_id, channel_id, message_id, payload_json,
                       status, created_at, updated_at
                   ) VALUES (?, ?, ?, ?, ?, ?, ?)""",
                (
                    str(intent.intent_id),
                    intent.source.channel_id,
                    intent.source.message_id,
                    intent.model_dump_json(),
                    intent.status.value,
                    now,
                    now,
                ),
            )
            await db.commit()

    async def get_intent(self, intent_id: UUID) -> TradingIntent | None:
        async with aiosqlite.connect(self._database_path) as db:
            db.row_factory = aiosqlite.Row
            cursor = await db.execute(
                "SELECT payload_json, status FROM intents WHERE intent_id = ?",
                (str(intent_id),),
            )
            row = await cursor.fetchone()
        if row is None:
            return None
        intent = TradingIntent.model_validate_json(row["payload_json"])
        return intent.model_copy(update={"status": IntentStatus(row["status"])})

    async def claim_for_execution(self, intent_id: UUID, user_id: int) -> bool:
        async with aiosqlite.connect(self._database_path) as db:
            cursor = await db.execute(
                """UPDATE intents
                   SET status = ?, decision_user_id = ?, updated_at = CURRENT_TIMESTAMP
                   WHERE intent_id = ? AND status = ?""",
                (IntentStatus.EXECUTING.value, user_id, str(intent_id), IntentStatus.PENDING.value),
            )
            await db.commit()
            return cursor.rowcount == 1

    async def mark_skipped(self, intent_id: UUID, user_id: int) -> bool:
        async with aiosqlite.connect(self._database_path) as db:
            cursor = await db.execute(
                """UPDATE intents
                   SET status = ?, decision_user_id = ?, updated_at = CURRENT_TIMESTAMP
                   WHERE intent_id = ? AND status = ?""",
                (IntentStatus.SKIPPED.value, user_id, str(intent_id), IntentStatus.PENDING.value),
            )
            await db.commit()
            return cursor.rowcount == 1

    async def mark_executed(self, intent_id: UUID, order_id: str) -> None:
        await self._set_terminal(intent_id, IntentStatus.EXECUTED, bybit_order_id=order_id)

    async def mark_failed(self, intent_id: UUID, error: str) -> None:
        await self._set_terminal(intent_id, IntentStatus.FAILED, error=error[:2000])

    async def _set_terminal(
        self,
        intent_id: UUID,
        status: IntentStatus,
        *,
        bybit_order_id: str | None = None,
        error: str | None = None,
    ) -> None:
        async with aiosqlite.connect(self._database_path) as db:
            await db.execute(
                """UPDATE intents
                   SET status = ?, bybit_order_id = ?, error = ?, updated_at = CURRENT_TIMESTAMP
                   WHERE intent_id = ?""",
                (status.value, bybit_order_id, error, str(intent_id)),
            )
            await db.commit()
</file>

<file path="src/cautious_crypto_bro/telegram_source.py">
from __future__ import annotations

import logging
from collections.abc import Awaitable, Callable
from datetime import datetime, timezone

from telethon import TelegramClient, events
from telethon.tl.custom.message import Message

from .domain import SourceMessage

logger = logging.getLogger(__name__)
MessageHandler = Callable[[SourceMessage], Awaitable[None]]


class TelegramSource:
    def __init__(self, *, api_id: int, api_hash: str, session_name: str,
                 channels: list[str | int], on_message: MessageHandler) -> None:
        self._client = TelegramClient(session_name, api_id, api_hash)
        self._channels = channels
        self._on_message = on_message

    async def start(self) -> None:
        await self._client.start()
        entities = []
        for channel in self._channels:
            entity = await self._client.get_entity(channel)
            entities.append(entity)
            logger.info("Watching Telegram source: %s", channel)

        @self._client.on(events.NewMessage(chats=entities))
        async def handle(event: events.NewMessage.Event) -> None:
            source = self._convert(event.message)
            if source is not None:
                await self._on_message(source)

    async def run_until_disconnected(self) -> None:
        await self._client.run_until_disconnected()

    async def disconnect(self) -> None:
        await self._client.disconnect()

    @staticmethod
    def _convert(message: Message) -> SourceMessage | None:
        # First vertical slice supports text and media captions only.
        text = (message.message or "").strip()
        if not text:
            logger.info("Ignoring message %s: no text/caption in MVP", message.id)
            return None
        chat = message.chat
        if chat is None:
            return None

        published_at = message.date
        if published_at.tzinfo is None:
            published_at = published_at.replace(tzinfo=timezone.utc)

        return SourceMessage(
            channel_id=message.chat_id,
            channel_title=getattr(chat, "title", None) or str(message.chat_id),
            channel_username=getattr(chat, "username", None),
            message_id=message.id,
            published_at=published_at,
            received_at=datetime.now(timezone.utc),
            text=text,
        )
</file>

<file path="tests/test_approval_render.py">
from datetime import datetime, timezone

from cautious_crypto_bro.approval_bot import ApprovalBot
from cautious_crypto_bro.domain import Entry, EntryType, Side, SourceMessage, TradingIntent


def test_render_contains_decision_context() -> None:
    intent = TradingIntent(
        source=SourceMessage(
            channel_id=-100123,
            channel_title="Trader & Co",
            channel_username="trader",
            message_id=99,
            published_at=datetime(2026, 9, 12, 18, 0, tzinfo=timezone.utc),
            received_at=datetime(2026, 9, 12, 18, 0, 1, tzinfo=timezone.utc),
            text="signal",
        ),
        symbol="ETHUSDT", side=Side.LONG,
        entry=Entry(type=EntryType.LIMIT, price=4000),
        stop_loss=3900, take_profit=4300,
        summary="Bounce from support.", confidence=0.9,
    )
    rendered = ApprovalBot._render(intent)
    assert "LONG ETHUSDT" in rendered
    assert "R:R:" in rendered
    assert "Open source message" in rendered
    assert "Trader &amp; Co" in rendered
</file>

<file path="tests/test_domain.py">
from datetime import datetime, timezone
import pytest
from pydantic import ValidationError

from cautious_crypto_bro.domain import Entry, EntryType, Side, SourceMessage, TradingIntent


def source() -> SourceMessage:
    return SourceMessage(
        channel_id=-100123,
        channel_title="Trader",
        channel_username="trader",
        message_id=42,
        published_at=datetime.now(timezone.utc),
        received_at=datetime.now(timezone.utc),
        text="LONG BTC 100k SL 98k TP 105k",
    )


def test_valid_long_limit_intent() -> None:
    intent = TradingIntent(
        source=source(), symbol="BTC/USDT", side=Side.LONG,
        entry=Entry(type=EntryType.LIMIT, price=100_000),
        stop_loss=98_000, take_profit=105_000,
        summary="Long BTC from support.", confidence=0.95,
    )
    assert intent.symbol == "BTCUSDT"


def test_long_geometry_rejects_stop_above_entry() -> None:
    with pytest.raises(ValidationError):
        TradingIntent(
            source=source(), symbol="BTCUSDT", side=Side.LONG,
            entry=Entry(type=EntryType.LIMIT, price=100_000),
            stop_loss=101_000, take_profit=105_000,
            summary="Invalid", confidence=0.5,
        )


def test_limit_requires_price() -> None:
    with pytest.raises(ValidationError):
        Entry(type=EntryType.LIMIT)


def test_market_rejects_price() -> None:
    with pytest.raises(ValidationError):
        Entry(type=EntryType.MARKET, price=100)
</file>

</files>
</file>

</files>
